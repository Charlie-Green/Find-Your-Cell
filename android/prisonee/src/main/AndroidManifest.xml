<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="by.zenkevich_churun.findcell.core">

    <application>

        <activity android:name="by.zenkevich_churun.findcell.prisonee.ui.PrisonerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>